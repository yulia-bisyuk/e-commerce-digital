import styled from 'styled-components';

const LogoWrapper = styled.div`
  width: fit-content;
  display: flex;
  align-items: center;
`;

const LogoText = styled.span`
  font-weight: 700;
  font-size: 24px;
  line-height: 1.14;
  color: white;
`;

export { LogoWrapper, LogoText };
